import React from 'react';
import { Container, Typography } from '@mui/material';

const UserDashboard = () => {
  return (
    <Container>
      <Typography variant="h4" my={4}>User Dashboard</Typography>
      {/* User-specific content here */}
    </Container>
  );
};

export default UserDashboard;
